package ${packageName};

import org.kohsuke.MetaInfServices;
import org.sedlakovi.celery.TaskLoader;

@MetaInfServices
public class ${taskName}Loader implements TaskLoader {
    @Override
    public ${packageName}.${taskName} loadTask() {
        return new ${packageName}.${taskName}();
    }
}